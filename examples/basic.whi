import "https://deno.land/x/p5wasm/p5_whi/p5.whi"

export fn setup() {
    createCanvas(500, 500)
}

export fn draw() {
    clear()
    fill(0x0000ff)
    rect(30, 40, 40, 40)
    circle(160, 60, 30)
}